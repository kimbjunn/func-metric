// Fill these in with YOUR Supabase project values, then commit.
// Dashboard -> Project Settings -> API.
// The anon key is PUBLIC by design; it is safe to expose because Row-Level
// Security (schema.sql) allows INSERT only — responses cannot be read back with it.
window.SUPABASE_URL = "https://oosxmyqzvfavqksrltyb.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_rePzKIcpIWRvunszbd4wxQ_mBqQtMKI";

// Optional: set false to disable DB writes (CSV-download-only fallback mode).
window.SURVEY_DB_ENABLED = true;
